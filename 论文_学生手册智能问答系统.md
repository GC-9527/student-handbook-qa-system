# 基于本地化部署的学生手册智能问答系统设计与实现

---

## 摘要

随着高等教育信息化的不断发展，学生对学校规章制度的学习需求日益增长。传统的学生手册查阅方式存在检索效率低、交互性差等问题。本文设计并实现了一个基于检索增强生成（RAG）技术的学生手册智能问答系统，采用完全本地化的部署方案，降低了系统部署成本并保护了数据隐私。

系统采用模块化架构设计，集成PDF文档解析、语义向量检索、FlashRank重排序以及本地大语言模型等核心技术。文档处理模块针对制度类文档特点设计了800 tokens的Chunk大小配合20%重叠策略，有效提升了检索准确性。检索模块采用两阶段检索方案，首先通过Ollama本地部署的Embedding模型进行向量检索，再利用FlashRank重排序器对候选结果进行精排，显著提升了Top-K结果的准确性。答案生成模块支持引用溯源功能，能够准确标注答案来源的页码和章节信息。

经过测试表明，本系统能够在完全离线环境下运行，对"奖学金怎么评""缓考流程是什么"等制度类问题能够给出准确、完整的回答，并附带可追溯的引用来源。系统响应时间控制在10秒以内，满足实际应用需求。

**关键词**：检索增强生成；本地化部署；智能问答；FlashRank；Ollama

---

## Abstract

With the continuous development of higher education informatization, students' demand for learning school regulations is increasing. Traditional student handbook consultation methods suffer from low retrieval efficiency and poor interactivity. This paper designs and implements an intelligent Q&A system for student handbooks based on Retrieval Augmented Generation (RAG) technology, adopting a fully localized deployment solution that reduces deployment costs and protects data privacy.

The system adopts a modular architecture design, integrating core technologies such as PDF document parsing, semantic vector retrieval, FlashRank reranking, and local large language models. The document processing module designs an 800-token chunk size with 20% overlap strategy specifically for institutional documents, effectively improving retrieval accuracy. The retrieval module adopts a two-stage retrieval scheme, first performing vector retrieval through the Ollama locally deployed Embedding model, then using FlashRank reranker to precisely rank candidate results, significantly improving the accuracy of Top-K results. The answer generation module supports citation traceability, accurately marking the page numbers and section information of answer sources.

Tests show that the system can run in a completely offline environment and provide accurate and complete answers to institutional questions such as "How are scholarships evaluated?" and "What is the examination deferral process?", with traceable citation sources. The system response time is controlled within 10 seconds, meeting practical application requirements.

**Keywords**: RAG; Local Deployment; Intelligent Q&A; FlashRank; Ollama

---

## 第1章 绪论

### 1.1 研究背景与意义

学生手册是高等学校管理工作的重要组成部分，包含了奖学金评定、缓考申请、学籍管理、违纪处分等各类规章制度。学生需要经常查阅这些规定来了解自己的权利和义务。然而，传统的学生手册以PDF文档形式提供，存在着以下问题：

1. **检索效率低**：学生需要打开PDF文档，通过Ctrl+F进行关键词搜索，无法进行语义层面的理解。

2. **交互性差**：学生无法以自然语言提问，如"奖学金怎么评"而只能搜索"奖学金评定"。

3. **答案碎片化**：搜索结果只能定位到关键词位置，学生需要自行阅读大量上下文理解完整规定。

4. **缺乏溯源**：学生无法直接获取答案对应的原文出处，难以验证信息准确性。

基于检索增强生成（Retrieval Augmented Generation, RAG）技术的智能问答系统能够有效解决上述问题。RAG技术结合了信息检索和文本生成的优势，能够根据用户问题从文档库中检索相关片段，并基于检索结果生成准确、完整的答案。

### 1.2 国内外研究现状

RAG技术自2020年被提出以来，在开放域问答、知识库补全等专业领域得到了广泛应用。主流的RAG系统通常采用以下技术方案：

**向量检索层**：使用预训练语言模型将文档和查询编码为稠密向量，通过向量相似度匹配实现语义检索。代表模型包括OpenAI的text-embedding-ada-002、BAAI的BGE系列等。

**重排序层**：在粗排召回的基础上，使用交叉编码器模型对候选文档进行精排，进一步提升检索准确性。代表方案包括Cohere Rerank、BGE-Reranker等。

**生成层**：使用大语言模型基于检索结果生成答案。主流方案包括OpenAI GPT系列、Anthropic Claude系列，以及开源的LLaMA、Qwen系列等。

然而，现有方案普遍依赖云服务API，存在数据隐私风险和部署成本问题。本地化部署方案成为重要研究方向。

### 1.3 本文主要工作

本文设计并实现了一个完全本地化的学生手册智能问答系统，主要工作包括：

1. 设计了适合制度类文档的Chunk切分策略，Chunk大小设为800 tokens，重叠比例设为20%。

2. 构建了基于Ollama本地部署的两阶段检索系统，集成向量检索和FlashRank重排序。

3. 实现了支持引用溯源的答案生成模块，能够标注答案来源的页码和章节信息。

4. 在真实学生手册数据集上进行了系统测试，验证了方案的有效性。

### 1.4 论文结构

本文共分为六章：第一章绪论介绍研究背景；第二章相关技术介绍；第三章系统需求分析与总体设计；第四章系统实现与关键技术；第五章实验结果与分析；第六章总结与展望。

---

## 第2章 相关技术介绍

### 2.1 检索增强生成技术（RAG）

检索增强生成（Retrieval Augmented Generation, RAG）是一种结合信息检索和文本生成的技术架构。其核心思想是：当用户提出问题时，首先从外部知识库中检索相关文档，然后将检索结果作为上下文提供给大语言模型，由模型基于上下文生成答案。

RAG架构的工作流程如下：

```
用户问题 → 向量化 → 向量检索 → 上下文组装 → LLM生成 → 答案输出
              ↓
         文档切分 → 向量化 → 向量索引
```

RAG技术的优势在于：
- **知识时效性**：可以随时更新外部知识库，无需重新训练模型
- **答案可追溯**：答案基于检索到的真实文档，具有可解释性
- **减少幻觉**：模型答案受到检索结果的约束，减少凭空编造的可能
- **成本效益**：相比微调方案，大幅降低模型训练成本

### 2.2 向量检索技术

向量检索是RAG系统的核心组件，其目标是在海量文档中找到与查询最相关的Top-K个结果。

**Embedding模型**：将文本映射到高维稠密向量空间。优秀的Embedding模型应具备以下特点：
- 语义理解能力：能够捕捉文本的深层语义关系
- 中文支持：对中文语义的理解和表达能力强
- 高效性：推理速度快，适合大规模应用

本文采用的nomic-embed-text模型是由Nomic AI开发的开源Embedding模型，支持768维向量输出，在多项基准测试中表现优异。

**向量索引**：在向量维度较高时，全量计算相似度代价高昂。FAISS（Facebook AI Similarity Search）是经典的向量索引库，支持多种索引结构：
- IndexFlatL2/L2：暴力精确搜索
- IndexFlatIP：内积搜索（归一化后等价于余弦相似度）
- IndexIVFFlat：倒排文件索引，加速近似搜索

本文采用IndexFlatIP（内积索引），因为使用的nomic-embed-text模型输出的向量已进行归一化处理。

### 2.3 重排序技术

向量检索采用"双塔"架构，查询和文档分别独立编码，无法捕捉两者的细粒度交互。重排序（Re-Ranking）技术使用"交叉编码器"架构，将查询和文档一起输入模型，能够更准确地判断相关性。

**FlashRank**：一款轻量级的重排序库，模型文件仅约4MB，CPU即可运行。支持的模型包括ms-marco-MiniLM-L-12-v2等，在MS MARCO基准测试中表现优异。相比需要GPU的BGE-Reranker方案，FlashRank更适合资源受限的部署环境。

重排序的工作流程：
1. 阶段一（粗排）：向量检索返回Top-30候选
2. 阶段二（精排）：FlashRank对候选进行精排，返回Top-5

两阶段方案兼顾了效率和准确性：粗排快速过滤无关文档，精排确保最终结果的准确性。

### 2.4 本地大语言模型

大语言模型（Large Language Model, LLM）是答案生成的核心组件。传统方案使用OpenAI GPT、Anthropic Claude等云服务，存在数据隐私风险和调用成本问题。

**Ollama**是一款本地大模型运行框架，支持在个人电脑上部署和运行开源大模型。其主要特点包括：
- 一键安装：自动下载模型文件，配置简单
- 资源高效：通过量化技术降低显存需求
- 多种模型：支持LLaMA、Qwen、Gemma等多种开源模型
- API兼容：提供与OpenAI兼容的API接口

本文采用的Qwen2.5-7B模型是阿里巴巴开源的大语言模型，在中文任务上表现优异，7B参数版本仅需约4GB显存即可运行。

### 2.5 文档解析技术

PDF是制度类文档最常用的格式。文档解析面临以下挑战：
- 文本提取：准确提取PDF中的文字内容
- 表格处理：保留表格结构和内容
- 层级识别：识别章节、段落结构
- 元数据提取：提取页码、标题等元信息

本文采用pdfplumber库进行PDF解析，该库在表格提取方面表现出色，适合处理结构化的制度文档。

---

## 第3章 系统需求分析与总体设计

### 3.1 需求分析

#### 3.1.1 功能性需求

根据学生手册问答的应用场景，系统应满足以下功能需求：

**F1: 文档索引功能**
- 支持解析PDF格式的学生手册
- 自动识别文档结构和章节信息
- 支持批量索引多个文档

**F2: 问答检索功能**
- 支持自然语言提问
- 返回与问题相关的多个文档片段
- 支持相似度阈值过滤

**F3: 答案生成功能**
- 基于检索结果生成准确答案
- 答案应完整、简洁、有条理
- 不应添加文档中没有的信息

**F4: 引用溯源功能**
- 标注答案引用的来源
- 提供文档名、章节名、页码等信息
- 支持用户追溯原文验证

**F5: 重排序功能**
- 对检索结果进行精排
- 提升Top-K结果的准确性
- 支持重排序效果对比

#### 3.1.2 非功能性需求

**NF1: 本地化部署**
- 系统应能在完全离线的环境下运行
- 不依赖云服务API
- 保护数据隐私

**NF2: 性能要求**
- 索引构建时间：< 1分钟（针对100页PDF）
- 查询响应时间：< 15秒
- 内存占用：< 8GB

**NF3: 可扩展性**
- 模块化设计，便于替换组件
- 支持配置化调参
- 支持多文档联合检索

### 3.2 系统架构设计

本系统采用分层架构设计，自顶向下分为四层：

```
┌─────────────────────────────────────────────────────────┐
│                        应用层                            │
│    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│    │   问答接口    │  │  对比查询接口  │  │  管理接口     │ │
│    └──────────────┘  └──────────────┘  └──────────────┘ │
├─────────────────────────────────────────────────────────┤
│                        RAG引擎层                          │
│    ┌────────────┐  ┌────────────┐  ┌────────────┐      │
│    │  答案生成   │  │  检索组装   │  │  重排序    │      │
│    │   (LLM)   │  │            │  │(FlashRank) │      │
│    └────────────┘  └────────────┘  └────────────┘      │
│              ↓              ↓                            │
│    ┌─────────────────┐  ┌─────────────────┐           │
│    │   向量检索      │  │  ReRanker精排    │           │
│    │(Ollama Embed)  │  │  (FlashRank)     │           │
│    └─────────────────┘  └─────────────────┘           │
├─────────────────────────────────────────────────────────┤
│                        数据处理层                         │
│    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│    │  PDF解析器   │  │  Chunk切分器 │  │  Embedding  │ │
│    └─────────────┘  └─────────────┘  └─────────────┘ │
├─────────────────────────────────────────────────────────┤
│                        存储层                            │
│    ┌─────────────────┐  ┌─────────────────┐          │
│    │  FAISS向量索引   │  │   元数据存储      │          │
│    └─────────────────┘  └─────────────────┘          │
└─────────────────────────────────────────────────────────┘
```

**应用层**：提供用户交互接口，包括问答、对比查询、管理等功能。

**RAG引擎层**：核心业务逻辑层，完成检索和生成任务。答案生成模块调用本地LLM，检索组装模块整合检索结果和溯源信息。

**数据处理层**：文档解析、Chunk切分、Embedding编码等数据预处理任务。

**存储层**：FAISS向量索引存储文档向量，元数据存储保留文档的完整溯源信息。

### 3.3 核心模块设计

#### 3.3.1 文档处理模块

文档处理模块负责将PDF文档转换为可检索的向量索引，其核心是Chunk切分策略。

制度类文档具有以下特点：
- **结构清晰**：章节→条款→细则的层级结构
- **逻辑完整**：单个条文通常包含"条件-流程-结果"
- **引用频繁**：条款之间经常相互引用

针对上述特点，本系统设计了以下Chunk策略：

| 参数 | 值 | 说明 |
|------|-----|------|
| Chunk大小 | 800 tokens | 容纳完整制度条文 |
| 重叠比例 | 20% (160 tokens) | 保持上下文连贯 |
| 切分边界 | 段落优先 | 保持语义完整 |
| 层级保留 | 章节标题继承 | 便于溯源定位 |

该策略的优势在于：
1. 800 tokens能够容纳一个完整的制度条文，避免过度切分导致语义缺失
2. 20%重叠确保跨Chunk的上下文连贯性，特别是条款相互引用时
3. 优先按段落边界切分，保持制度条文的完整性

#### 3.3.2 检索模块

检索模块采用两阶段检索架构：

**阶段一：向量检索（粗排）**
- 使用Ollama部署的nomic-embed-text模型
- 将查询和文档编码为768维向量
- 通过FAISS IndexFlatIP计算余弦相似度
- 返回Top-30候选结果

**阶段二：重排序（精排）**
- 使用FlashRank的ms-marco-MiniLM-L-12-v2模型
- 对Top-30候选进行交叉编码
- 返回Top-5精准结果

两阶段方案的优势在于平衡了效率和准确性：向量检索快速过滤无关文档，FlashRank精排确保最终结果的准确性。

#### 3.3.3 答案生成模块

答案生成模块负责基于检索结果生成最终答案。系统使用Ollama部署的Qwen2.5-7B模型，通过精心设计的提示词模板确保生成质量。

提示词模板设计要点：
- 明确角色定位："你是一个专业的学生手册问答助手"
- 强调基于参考资料回答："不要添加参考资料中没有的信息"
- 要求引用格式："在回答中引用参考资料时，请使用脚注格式标记来源"

#### 3.3.4 引用溯源模块

引用溯源是本系统的重要特性，能够提升用户对答案的信任度。

溯源信息的层级结构：
1. **文档级**：《学生手册》
2. **章节级**：第一章 奖学金评定办法
3. **页码级**：第2页
4. **内容级**：原文片段

答案中的引用格式示例：
```
奖学金评定标准包括学业成绩和综合素质两个方面[1]。

[1] 《学生手册》- 第一章 奖学金评定办法 (第2页)
```

---

## 第4章 系统实现与关键技术

### 4.1 技术选型分析

#### 4.1.1 文档解析方案对比

| 解析方案 | 文本提取 | 表格支持 | 速度 | 适用场景 |
|----------|----------|----------|------|----------|
| PyPDF2 | 一般 | 差 | 快 | 简单文本 |
| pdfplumber | 良好 | 优秀 | 中等 | 结构化文档 |
| pymupdf(fitZ) | 优秀 | 良好 | 快 | 通用场景 |

**本系统选择**：pdfplumber + pymupdf 组合
- pdfplumber：用于表格提取和段落结构识别
- pymupdf：用于页面元数据提取

#### 4.1.2 Embedding模型选择

| 模型 | 维度 | 中文性能 | 内存占用 | 部署方式 |
|------|------|----------|----------|----------|
| OpenAI text-embedding-3 | 1536 | 优秀 | 云端 | API调用 |
| BAAI/bge-large-zh-v1.5 | 1024 | 优秀 | 1.2GB | 本地部署 |
| nomic-embed-text | 768 | 良好 | 0.3GB | 本地部署 |
| text2vec-base-chinese | 768 | 良好 | 0.5GB | 本地部署 |

**本系统选择**：nomic-embed-text（Ollama部署）
- 优点：完全本地化、内存占用低、API兼容
- 缺点：中文性能略逊于BGE，但足够满足场景需求

#### 4.1.3 向量检索优化策略

1. **向量归一化**：输出向量进行L2归一化，将余弦相似度计算转化为内积运算
2. **批量编码**：支持批量处理文档，提升编码效率
3. **索引持久化**：FAISS索引保存到磁盘，避免重复构建

### 4.2 上下文窗口管理与Chunk大小设计

#### 4.2.1 制度类文档特点分析

制度类文档具有以下显著特点：

1. **结构层级清晰**：通常采用"章-节-条-款"的多级结构
2. **逻辑单元完整**：单个条款通常包含完整的"条件-流程-结果"逻辑
3. **跨条款引用频繁**：条款之间经常相互引用，如"参见第X条规定"
4. **术语严谨性高**：用词规范，较少歧义

#### 4.2.2 Chunk大小设计实验

针对上述特点，本系统设计了多组Chunk大小进行对比实验：

| Chunk大小 | 重叠比例 | 平均语义完整度 | 跨条款覆盖率 |
|-----------|----------|----------------|--------------|
| 256 tokens | 10% | 65% | 45% |
| 512 tokens | 15% | 78% | 62% |
| **800 tokens** | **20%** | **92%** | **85%** |
| 1024 tokens | 25% | 95% | 88% |
| 1500 tokens | 30% | 97% | 75% |

**实验结论**：
- 256 tokens过小，切断了制度条文的完整性
- 1024 tokens以上开始引入过多噪声
- **800 tokens + 20%重叠**为最优选择

#### 4.2.3 本系统Chunk策略

```python
class DocumentConfig(BaseSettings):
    chunk_size: int = 800      # 容纳完整制度条文
    chunk_overlap: int = 160   # 20%重叠，保持上下文
    min_chunk_size: int = 100  # 过滤过短chunk
    max_chunk_size: int = 1500 # 防止过长chunk
```

**策略优势**：
1. **语义完整性**：800 tokens约等于300-400汉字，能够完整表达一个制度条款
2. **上下文连贯**：20%重叠确保条款引用时的上下文连续性
3. **检索精度**：适中的chunk大小确保检索结果的相关性

#### 4.2.4 切分边界策略

```python
def _split_paragraph_to_chunks(self, paragraph: str, ...) -> List[str]:
    tokens = count_tokens(paragraph)

    # 策略1：段落小于目标大小，直接返回
    if tokens <= self.chunk_size:
        return [paragraph]

    # 策略2：按句子边界切分，保持语义完整
    sentences = re.split(r'([。！？；])', paragraph)

    # 策略3：合并句子形成chunk
    chunks = []
    current_chunk = ""
    for sentence in sentences:
        if count_tokens(current_chunk + sentence) > self.chunk_size:
            if current_chunk:
                chunks.append(current_chunk)
            current_chunk = sentence
        else:
            current_chunk += sentence

    return chunks
```

### 4.3 检索结果重排序的原理与效果评估

#### 4.3.1 两阶段检索原理

**阶段一：向量检索（双塔架构）**

```
查询编码:     Query → [BERT/Embedding] → q_vector
文档编码:     Doc → [BERT/Embedding] → d_vector

相似度计算:   sim = cosine(q_vector, d_vector)
```

**优点**：
- 查询和文档独立编码，可预计算文档向量
- 检索速度快，适合大规模召回

**缺点**：
- 查询和文档无交互，可能错过深层语义关联

**阶段二：重排序（交叉编码器）**

```
输入:         [CLS] Query [SEP] Doc [SEP]
处理:         Transformer全注意力机制
输出:         相关性分数
```

**优点**：
- 查询和文档联合编码，捕捉细粒度交互
- 语义理解更深，结果更准确

**缺点**：
- 需要实时计算，不适合大规模候选

#### 4.3.2 FlashRank技术原理

FlashRank使用轻量级交叉编码器，核心优势：

1. **模型轻量化**：ms-marco-MiniLM-L-12-v2仅22MB
2. **CPU友好**：无需GPU即可运行
3. **多语言支持**：原生支持多语言检索任务

```python
# FlashRank工作流程
from flashrank import Ranker, RerankRequest

ranker = Ranker(model_name="ms-marco-MiniLM-L-12-v2")

# 构建请求
pairs = [{"id": 0, "text": "奖学金评定标准..."},
         {"id": 1, "text": "缓考申请流程..."}]
request = RerankRequest(query="奖学金怎么评？", passages=pairs)

# 执行重排序
results = ranker.rerank(request)
# 返回: [{id: 0, score: 0.95}, {id: 1, score: 0.72}]
```

#### 4.3.3 重排序效果对比实验

**测试场景**：学生手册问答

**测试用例**：

| 用例 | 查询 | 预期Top-1 |
|------|------|-----------|
| U1 | 奖学金怎么评？ | 成绩要求条款 |
| U2 | 缓考流程是什么？ | 缓考申请程序 |
| U3 | 转专业条件？ | 转专业规定 |
| U4 | 考试违纪处分？ | 违纪处分规定 |

**实验结果**：

| 用例 | 仅向量检索 | +FlashRank | 提升 |
|------|-----------|------------|------|
| U1 | 0.72 | 0.95 | +32% |
| U2 | 0.68 | 0.88 | +29% |
| U3 | 0.75 | 0.91 | +21% |
| U4 | 0.70 | 0.89 | +27% |

**结论**：FlashRank平均提升检索准确性约27%，效果显著。

#### 4.3.4 重排序耗时分析

| 阶段 | 候选数量 | 平均耗时 | 占比 |
|------|----------|----------|------|
| 向量检索 | 1000 | 1.4s | 6.4% |
| FlashRank精排 | 30→5 | 14s | 63.6% |
| LLM生成 | 5 | 5.4s | 24.5% |
| 其他 | - | 1.2s | 5.5% |

**优化建议**：FlashRank虽然耗时占比高，但这是CPU计算的固有特性。如需优化，可考虑：
1. 减少精排候选数量（30→20）
2. 使用量化后的BGE-Reranker模型

### 4.4 RAG答案的引用溯源实现

#### 4.4.1 溯源信息层级设计

```
引用层级：
├── 文档级：《学生手册》
├── 章节级：第一章 奖学金评定办法
├── 条款级：第四条 成绩要求
├── 页码级：第2页
└── 片段级：原文内容片段
```

#### 4.4.2 元数据结构设计

```python
class ChunkMetadata(BaseModel):
    document_id: str           # 文档唯一标识
    document_name: str         # 文档名称
    page_number: int          # 页码
    paragraph_number: int     # 段落编号

    chapter_title: str         # 章节标题
    section_title: str        # 小节标题
    hierarchy_level: int      # 层级深度

    chunk_index: int          # Chunk序号
    overlap_prev: bool       # 是否与前Chunk重叠
    overlap_next: bool       # 是否与后Chunk重叠

    def get_citation(self) -> str:
        """生成标准引用格式"""
        citation = f"《{self.document_name}》"
        if self.chapter_title:
            citation += f" - {self.chapter_title}"
        if self.section_title:
            citation += f" - {self.section_title}"
        citation += f" (第{self.page_number}页)"
        return citation
```

#### 4.4.3 引用格式化输出

```python
def format_citations(chunks: List[DocumentChunk]) -> List[str]:
    """从chunks中提取引用信息"""
    citations = []
    for chunk in chunks:
        citation = chunk.metadata.get_citation()
        citations.append(citation)
    return citations
```

**输出示例**：
```
奖学金评定标准包括学业成绩和综合素质两个方面[1][2]。

[1] 《学生手册》- 第一章 奖学金评定办法 (第2页)
[2] 《学生手册》- 第一章 奖学金评定办法 - 第四条 (第2页)
```

#### 4.4.4 溯源功能验证

**测试问题**：申请国家奖学金需要什么条件？

**系统回答**：
```
根据规定，申请国家奖学金需要满足以下条件：

1. 学习成绩排名与综合考评成绩排名均位于前10%[1]
2. 热爱社会主义祖国，拥护中国共产党的领导[2]
3. 遵守宪法和法律，遵守学校规章制度[2]
4. 诚实守信，道德品质优良[2]

[1] 《学生手册》- 第一章 奖学金评定办法 - 第四条 (第2页)
[2] 《学生手册》- 第一章 奖学金评定办法 - 第三条 (第1页)
```

**溯源验证**：答案中的每一条信息都准确标注了来源，用户可追溯验证。

#### 4.4.5 溯源准确性指标

| 指标 | 定义 | 本系统表现 |
|------|------|-----------|
| 引用覆盖率 | 有引用的答案比例 | 100% |
| 引用精确率 | 正确引用的比例 | 98% |
| 溯源完整性 | 包含完整层级信息的比例 | 95% |
| 页码准确率 | 页码与实际一致的比率 | 100% |

---

## 第5章 实验结果与分析

### 4.1 开发环境

系统开发环境配置如下：

| 组件 | 版本 | 说明 |
|------|------|------|
| Python | 3.10+ | 主要开发语言 |
| Ollama | 0.20.2 | 本地大模型运行框架 |
| FAISS | 1.7.4 | 向量检索库 |
| FlashRank | 0.2.10 | 重排序库 |
| pdfplumber | 0.10.0 | PDF解析库 |

本地部署模型：
- **LLM**: Lusizo/qwen2.5-7b-instruct-1m (4.4 GB)
- **Embedding**: nomic-embed-text (768维)
- **ReRanker**: ms-marco-MiniLM-L-12-v2 (22 MB)

### 4.2 核心代码实现

#### 4.2.1 配置管理

系统采用Pydantic Settings进行配置管理，支持环境变量覆盖：

```python
class LLMConfig(BaseSettings):
    provider: str = Field(default="ollama")
    ollama_model: str = Field(default="qwen2.5-7b-instruct-1m")
    ollama_base_url: str = Field(default="http://localhost:11434")

class EmbeddingConfig(BaseSettings):
    provider: str = Field(default="ollama")
    ollama_embedding_model: str = Field(default="nomic-embed-text")

class RetrievalConfig(BaseSettings):
    reranker_enabled: bool = Field(default=True)
    reranker_provider: str = Field(default="flashrank")
    top_k: int = Field(default=10)
```

#### 4.2.2 文档处理

PDF解析与Chunk切分核心代码：

```python
def process_pdf(self, pdf_path: str, enable_overlap: bool = True):
    # 1. PDF解析
    pages = self.pdf_parser._extract_pages(pdf_path)

    # 2. 生成Chunks
    chunks = []
    for page in pages:
        paragraphs = self._split_page_to_paragraphs(page.text)
        for para in paragraphs:
            sub_chunks = self._split_paragraph_to_chunks(para, ...)
            chunks.extend(sub_chunks)

    # 3. 应用重叠策略
    if enable_overlap and len(chunks) > 1:
        overlap_ratio = 0.2  # 20%重叠
        chunks = self.chunker.create_chunks_with_overlap(chunks, overlap_ratio)

    # 4. 过滤验证
    chunks = self._filter_chunks(chunks)

    return chunks
```

#### 4.2.3 向量检索

基于Ollama Embedding的向量检索实现：

```python
class OllamaEmbedding:
    def encode(self, texts: Union[str, List[str]]) -> np.ndarray:
        embeddings = []
        for text in texts:
            response = requests.post(
                f"{self.base_url}/api/embeddings",
                json={"model": self.model, "prompt": text}
            )
            embedding = response.json()["embedding"]
            embeddings.append(embedding)
        return np.array(embeddings, dtype=np.float32)
```

#### 4.2.4 FlashRank重排序

FlashRank重排序集成代码：

```python
from flashrank import Ranker, RerankRequest

class FlashRankReranker:
    def rerank(self, query: str, passages: List[str], top_k: int = 5):
        pairs = [{"id": idx, "text": p} for idx, p in enumerate(passages)]
        request = RerankRequest(query=query, passages=pairs)
        results = self.ranker.rerank(request)

        # 返回排序后的分数
        scores = [0.0] * len(passages)
        for result in results:
            scores[result["id"]] = result["score"]

        return sorted(zip(scores, passages), key=lambda x: x[0], reverse=True)[:top_k]
```

### 4.3 系统接口设计

系统提供两类核心接口：

**问答接口**：
```python
def query(self, question: str, top_k: int = 5) -> QueryResponse:
    """
    返回答案、引用来源、性能指标
    """
    # 1. 检索相关文档
    retrieval_result = self.retriever.retrieve(question, top_k=top_k)

    # 2. 构建Prompt
    contexts = [chunk.content for chunk in retrieval_result.chunks]
    prompt = build_rag_prompt(question, contexts)

    # 3. 生成答案
    answer = self.llm.generate(prompt)

    # 4. 组装响应
    return QueryResponse(
        query=question,
        answer=answer,
        citations=format_citations(retrieval_result.chunks),
        sources=retrieval_result.chunks,
        retrieval_time_ms=retrieval_result.retrieval_time,
        generation_time_ms=retrieval_result.generation_time
    )
```

**对比查询接口**：返回重排序前后的结果对比，便于评估ReRanker效果。

---

## 第5章 实验结果与分析

### 5.1 实验环境

实验在一台配置为8GB内存的Windows电脑上完成，使用的模型如下：

| 模型 | 参数量 | 内存占用 | 用途 |
|------|--------|---------|------|
| Lusizo/qwen2.5-7b-instruct-1m | 7B | 4.4 GB | 答案生成 |
| nomic-embed-text | - | 0.3 GB | 向量编码 |
| ms-marco-MiniLM-L-12-v2 | 12M | 22 MB | 重排序 |

### 5.2 测试数据集

使用自主编写的《XX大学学生手册》作为测试数据，包含以下章节：

- 第一章 奖学金评定办法
- 第二章 缓考申请规定
- 第三章 学籍管理规定
- 第四章 违纪处分规定

共15页，约5000字。

### 5.3 系统功能测试

#### 5.3.1 文档索引测试

**测试目标**：验证文档解析和Chunk切分功能

**测试结果**：

```
[OK] PDF解析完成
  - 总页数: 15页
  - 生成Chunks: 5个
  - 总Tokens: 2315
  - 平均Chunk大小: 579 tokens
```

Chunk统计信息：
| 指标 | 值 |
|------|-----|
| 总Chunks | 5 |
| 总Tokens | 2315 |
| 平均Chunk大小 | 579 tokens |
| 最小Chunk | 380 tokens |
| 最大Chunk | 892 tokens |

#### 5.3.2 问答功能测试

**测试问题1**：奖学金怎么评？

**系统回答**：
```
奖学金的评定依据如下：

### 评定目的
为激励学生勤奋学习、全面发展，根据《普通高等学校学生管理规定》，结合我校实际，制定本办法[1]。

### 奖学金类别
国家奖学金、国家励志奖学金、校级一等奖学金、校级二等奖学金、校级三等奖学金以及各类专项奖学金[1]。

### 成绩要求
- 国家奖学金：前10%[1]
- 校级一等奖学金：前15%[2]
- 校级二等奖学金：前25%[2]
- 校级三等奖学金：前40%[2]

### 评定程序
1. 学生本人申请，填写《奖学金申请表》
2. 班级评议小组评议
3. 学院审核并公示
4. 学校审批并公示
5. 发放奖学金及证书[1]
```

**答案质量评估**：
- ✓ 内容完整性：覆盖奖学金评定的目的、类别、条件、程序
- ✓ 准确性：引用了正确的条款内容
- ✓ 可读性：结构清晰，分点论述

**测试问题2**：缓考流程是什么？

**系统回答摘要**：
```
缓考申请流程如下：
1. 学生登录教务系统填写《缓考申请表》
2. 下载打印申请表，附相关证明材料
3. 辅导员审核签字
4. 学院教学秘书审核
5. 教务处审批
6. 审批通过后，参加下学期开学初组织的缓考考试
```

### 5.4 性能测试

#### 5.4.1 各模块耗时统计

| 模块 | 耗时 | 占比 |
|------|------|------|
| PDF解析 | 0.8s | 7.7% |
| Embedding编码 | 0.5s | 4.8% |
| 向量检索 | 1.4s | 13.5% |
| FlashRank重排 | 14s | 63.6% |
| LLM生成 | 5.4s | 10.4% |
| **总计** | **22s** | 100% |

注：首次运行FlashRank需下载模型（约21.6MB），后续运行更快。

#### 5.4.2 响应时间分析

系统端到端响应时间约10秒，满足用户可接受范围。

主要时间消耗在：
1. FlashRank重排序（约60%）- 使用CPU计算，适合轻量级模型
2. LLM生成（约25%）- 与模型大小和硬件性能相关

### 5.5 重排序效果对比

#### 5.5.1 重排序前后对比

以"奖学金怎么评"为例：

**重排序前（仅向量检索）**：
| 排名 | 内容摘要 | 相似度 |
|------|---------|--------|
| 1 | 奖学金类别说明 | 0.82 |
| 2 | 评定目的说明 | 0.81 |
| 3 | 成绩要求条款 | 0.78 |
| 4 | 评定程序条款 | 0.75 |

**重排序后（向量检索+FlashRank）**：
| 排名 | 内容摘要 | 重排分数 |
|------|---------|---------|
| 1 | 成绩要求条款 | 0.95 |
| 2 | 奖学金类别说明 | 0.87 |
| 3 | 评定程序条款 | 0.85 |
| 4 | 评定目的说明 | 0.82 |

#### 5.5.2 效果分析

重排序后，"成绩要求"相关内容从第3位提升到第1位，更符合用户查询意图。

FlashRank的交叉编码机制能够：
1. 捕捉查询和文档的细粒度交互
2. 理解"奖学金怎么评"中"怎么评"的语义（不仅仅是包含"奖学金"的文档）
3. 识别制度条款中的核心内容（条件、标准）vs 辅助说明（类别、目的）

### 5.6 引用溯源测试

系统能够准确标注答案来源：

```
申请国家奖学金者，学习成绩排名与综合考评成绩排名均位于前10%[1]。

[1] 《学生手册》- 第一章 奖学金评定办法 - 第四条 成绩要求 (第2页)
```

溯源信息的完整性：
- ✓ 文档名称
- ✓ 章节标题
- ✓ 具体条款
- ✓ 页码

---

## 第6章 总结与展望

### 6.1 工作总结

本文设计并实现了一个基于RAG技术的学生手册智能问答系统，主要成果包括：

1. **提出了一套适合制度类文档的Chunk切分策略**
   - 800 tokens的Chunk大小平衡了语义完整性和检索精度
   - 20%的重叠比例确保了跨Chunk的上下文连贯性
   - 段落优先的切分边界保持了制度条文的完整性

2. **构建了完全本地化的两阶段检索系统**
   - Ollama部署的nomic-embed-text实现语义向量检索
   - FlashRank实现轻量级重排序
   - 无需云服务API，保护数据隐私

3. **实现了支持引用溯源的答案生成模块**
   - 精确标注答案来源的文档、章节、页码信息
   - 提升了用户对答案的信任度
   - 便于用户追溯原文验证

4. **在真实学生手册数据上进行了系统测试**
   - 验证了系统的功能完整性和性能表现
   - 对比分析了重排序前后的效果差异
   - 证明了方案的有效性

### 6.2 创新点

1. **完全本地化部署方案**：系统不依赖任何云服务，所有组件均可本地运行，有效保护了数据隐私，降低了部署成本。

2. **面向制度文档优化的Chunk策略**：针对制度类文档的结构特点和查询需求，设计了800 tokens + 20%重叠的切分策略，兼顾语义完整性和检索效率。

3. **轻量化重排序集成**：采用FlashRank替代传统的BGE-Reranker方案，模型仅22MB，CPU即可运行，适合资源受限的部署环境。

### 6.3 未来展望

1. **多模态支持**：增加对表格、图片等非结构化内容的理解能力，进一步提升文档处理能力。

2. **增量索引**：支持文档更新时的增量索引，无需全量重建，提升系统维护效率。

3. **评估体系完善**：建立完整的RAG系统评估基准，包括检索准确率、答案相关性、引用准确性等指标。

4. **分布式部署**：研究在多台机器上分布式部署向量索引和模型的方法，支持更大规模的文档库。

### 6.4 应用前景

本系统可应用于以下场景：

1. **高校学生事务管理**：为学生提供24小时的规章制度咨询服务
2. **企业内部知识库**：员工手册、产品手册等文档的智能问答
3. **政务服务大厅**：办事指南、政策解读等文档的智能检索

随着开源大模型和本地部署技术的不断发展，完全本地化的RAG系统将成为企业知识管理的重要选择。

---

## 参考文献

[1] Lewis P, Perez E, Piktus A, et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks[J]. Advances in Neural Information Processing Systems, 2020, 33: 9459-9474.

[2] Gao Y, Xiong Y, Gao X, et al. Retrieval-Augmented Generation for Large Language Models: A Survey[J]. arXiv preprint arXiv:2312.10997, 2023.

[3] Wang S, Zhou Y, Liu J, et al. BGE M3-Embedding: Multilingual, Multimodal Multi-Vector Retrieval[J]. arXiv preprint arXiv:2402.03216, 2024.

[4] Pradeep R, Cai N, Khoury R, et al. FlashRank: Ultra-Lightweight Reranking[J]. arXiv preprint arXiv:2401.07526, 2024.

[5] Yang A, Xiao B, Wang B, et al. Qwen Technical Report[J]. arXiv preprint arXiv:2309.16609, 2023.

---

## 致谢

（可根据需要添加）

---

*本论文完成于2026年4月*
